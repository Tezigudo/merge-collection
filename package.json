{
  "name": "merge-collection",
  "module": "src/merge.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun bundle src/merge.ts"
  }
}
